[package]
name = "yukifile"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "yukifile"
path = "src/lib.rs"

[dependencies]
blake3 = "1.8.7"
rusqlite = { version = "0.40.2", features = ["bundled"] }
zip = { version = "8.6.0", default-features = false, features = ["deflate"] }

[dev-dependencies]
